# Phase 3 retest @ 9990a8b (UI only)

Stack running, reseeded. Prospects id cmtnunxuq0002qhvg6vf0iurw (after restart/reseed), segment "AI companies". Login demo@gtmai.dev / demo1234.

1. /sequences → open seeded sequence → edit step 1 subject to "Quick idea for {{company.name}} RETEST" → preview-card (sequences/page.tsx:118-223) shows "Quick idea for Analytical Engines RETEST" without save/reload. FAIL if preview text unchanged.
2. /campaigns → "New campaign" dialog (page.tsx:282-352): source Segment → AI companies → Create → card appears with 5 enrollments. Start → within ~10s enrollments show `sent`. Click enrollment row (openEnrollment:248) → drawer with message timeline (subject, direction). Simulate reply → row `replied` and card stats (replied count) change within 5s poll (page.tsx:81) without F5.
3. /ads → Create dialog with segment AI companies, platforms mock+meta → Sync → chip mock synced(5), meta failed with visible text "No connection for meta" (ads/page.tsx:101).
4. /crm → "New sync job" → editor drawer: source segment, destination mock, mapping editor rows (crm/page.tsx:337-391) add/modify a mapping → Save → Run now → toggle runs (175) → run history shows "5 synced · …". Seeded job runs also show history.
5. /credits → Budgets form (credits/page.tsx:66-121) scope workspace, daily, limit 1 → Create → budget listed. Prospects → Work email ▶ Run → grid chips `skipped` appear live (no reload) via SSE (worker main.ts:120). /credits Alerts card shows `budget_exceeded` alert. Delete budget via Remove button → budgets list empty. Rerun → done.
6. /workflows → seeded card ▶ Run → window.prompt "Signal input JSON" appears (workflows/page.tsx:76); submit → Run queued.
Note layout problems throughout.
