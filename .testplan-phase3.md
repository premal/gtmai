# Phase 3 fresh-stack pass (f4bd761)

Setup done: fresh docker volume, migrate, seed (API key `gtm_2_TeOYda1Xxk_ALoY0CmQs74oa0ebETu`), api/worker/web running, CLI built. Prospects id `cmtnrdd550007qhu00bine84r`, segment `cmtnrdd4v0001qhu0kggrhgtt-ai-companies`.
Note: web create buttons hard-code payloads (ads platforms=['mock'], crm provider mock/empty source, campaigns contactIds=[] no segment) — segment/meta/webhook variants will be done via Swagger UI at :4000/docs (Authorize with gtm_ key), which is a visible UI, and results verified in the web pages.

S1 /sequences: card "…" seeded sequence visible with 2 steps; click it → step editor shows step 1 subject "Quick idea for {{company.name}}", step 2 delay 24h, preview text "Hello Ada at Analytical Engines" (or similar rendered sample). "+ Create sequence" → prompt → new card appears in list (DB Sequence count 1→2).
C1 /campaigns: seeded "Signal follow-up campaign" active; open → enrollments list shows statuses incl. `replied` (1) and sent/active; click an enrollment → message timeline drawer shows message(s) with subject/direction.
C2 Create campaign from segment: Swagger POST /campaigns {name:"AI outreach", sequenceId:<seeded>, segmentId:"…-ai-companies", contactIds:[]} → 201 with 5 enrollments; /campaigns shows it; Start → within ~10s enrollments show `sent`/messages exist (DB Message count +5, Enrollment.status); Simulate reply on one → status `replied`. Delayed: Redis `bull:outbound` delayed set has jobs `outbound:<enrollment>:2` with delay ~24h (score ≈ now+86400s); no step-2 messages sent (DB Message where stepPosition 2 = 0; per enrollment messages = 1).
A1 /ads: seeded "Demo synced audience" card with chip `mock: synced (N)` N>0.
A2 Swagger POST /ads/audiences {name:"AI seg audience", segmentId, platforms:["mock","meta"]} → /ads shows card with "mock, meta"; Sync now → after ~5s chips: `mock: synced (5)` and `meta: failed` with error text "No connection for meta" visible (or in DB AdPlatformSync.error).
R1 /crm: seeded "Demo CRM mock sync" card with run history (runs list, counts). Run now → new run row with counts (created/updated/failed) appears.
R2 Webhook job (worker resolves url from a Connection with provider "webhook", credentials.url — create it via /connections UI or Swagger POST /connections first; no mapping editor exists in crm/page.tsx → mapping only via Swagger body): start local listener `python3 -m http.server`-like (nc/py) on :4555 logging POST; Swagger POST /crm/jobs {name:"Webhook job", source:{kind:'segment',id:seg}, destination:{provider:'webhook', object:'contact', fieldMapping:{email:'email',firstname:'firstName'}, upsertKey:'email', url:'http://localhost:4555/hook'}} (check bodySchema for url location) → Run now → run counts 5, listener received POSTs with mapped fields. Mapping editor: verify UI exposes fieldMapping editing (crm page); if no editor exists → report FAIL/untested.
K1 /settings: "Create API key" → name prompt → plaintext `gtm_…` shown once (screenshot); reload → only prefix. curl -H "Authorization: Bearer <new key>" localhost:4000/tables → 200 with Prospects JSON. CLI: `node packages/cli/dist/index.js login --api-key <key>` then `tables list` → prints Prospects.
U1 /credits: Usage dashboard shows per-day bars (today's spend >0), Spend by table (Prospects), Spend by provider (mock), Budgets list empty. Swagger POST /usage/budgets {scope:"workspace",limit:1,period:"daily"} → reload /credits → Budgets card "workspace 1 credits / daily". Prospects → Work email column ▶ Run → cells DB status skipped error "budget exceeded" (count 20), no new ledger rows. Swagger DELETE budget → rerun → all done, ledger +20. GET /usage/alerts lists ≥1 alert (budget exceeded) — also check if /credits shows alerts. POST /usage/rollup → {queued:true,jobId}; worker log shows rollup processed / UsageRollup rows exist.
P2 Regression: /workflows list loads seeded workflow; card Run → "Run queued" toast, run appears done. /audiences → Import from table Prospects → toast with counts.
