# docs

- `bazel-pilot.md` — the Bazel build/test layout: target kinds (hermetic
  vitest/eslint vs local `sh_test` wrappers), the Prisma/dist stamp
  mechanism, and `bazel-affected.sh` selection semantics.
