# PR #5 @ b845ff5 — Team roles / access / credit limits (UI, running stack)

Users (demo ws `cmtnrdd4v0001qhu0kggrhgtt`): demo@gtmai.dev owner (membership `cmtnrdd4v0003qhu0xzr8wyj7`), editor@gtmai.dev editor, viewer@gtmai.dev viewer; pw demo1234. Default workbook `7d0e6264…` (Hunter table `cmtorv0hq…` 80 rows, Prospects 21 rows with mock "Work email"). Q3 campaigns wb `cmtouc9oy…` (0 tables).
Code: login `apps/web/app/login/page.tsx` (no Register); Team in `settings/page.tsx:136-300` (invite form ＋ Invite, role select disabled when last admin, Remove confirm, Pending invites Copy link/Revoke); invite page `invite/[token]/page.tsx` (h1 "Join <ws> as <role>", "This invite has expired." for accepted/expired, error p); API `team.controller.ts` ("Workspace needs at least one admin"); workbook page `workbooks/[id]/page.tsx:347-370` (🔒 Restricted badge, Access…, Credit limit… for admin; `limit: N/period` badge), access modal radios 509-519 with collaborator checkboxes (non-admins only); `editBudget` → POST /usage/budgets scope `workbook:<id>`; worker `main.ts:124` cell error "Credit limit reached for workbook <name>"; API `workbook-access.ts` 403 "No access to this workbook"; sidebar role `auth.tsx:98`; canEdit gating page.tsx:59, table-workspace.tsx:187.

1. /login: only "Log in" (no Register button). Admin → /settings → Team lists demo (owner), editor (editor), viewer (viewer) = 3 members. FAIL if Register present or count ≠ 3.
2. Invite newuser@gtmai.dev role editor → ＋ Invite → row appears under Pending invites with Copy link. Open the link (read URL from DOM/DB) in a new tab after Sign out → h1 "Join Demo Workspace as editor" → name "New User", password demo1234 → Accept → lands in app, sidebar shows email + "editor". Sign out, admin → Team shows 4 members incl. newuser editor; Pending invites empty. Re-open same invite URL → "This invite has expired." (accepted) and Accept fails with error; bogus token → error text, no crash.
3. Admin sets newuser role → viewer (select). Login as newuser → sidebar "viewer"; home has no ＋Folder/＋Workbook/⋮/＋Tag; workbook page no ＋Add table/Access/Credit limit; table page no Run column/＋Add column/＋Row/Import/Find people/▶ Run in headers; cell inputs read-only. Admin: role select and Remove for demo disabled (only admin). Attempt via editor/viewer irrelevant. FAIL if controls visible for viewer or demo demotable.
4. Login editor → ＋Workbook "Editor wb" created; inside ＋Add table "Editor tbl" → opens; Hunter table ▶ Run Work email on a selected row → done (mock). Editor's Settings: no Team management (no invite form/role selects), API key create/revoke hidden; workbook ⋮ has no Access…/Credit limit…; Connections page has no Add/Delete controls.
5. Admin → Q3 campaigns workbook → Access… → "Admins and specific collaborators", no collaborators → Save → 🔒 Restricted badge. Editor: home lacks Q3 campaigns card; search "Q3" gives none; direct /workbooks/cmtouc9oy… → in-app error/toast "No access to this workbook" (not crash). Admin: Access… → check editor@gtmai.dev → Save. Editor reload → Q3 visible and opens. Admin Default workbook still opens, Hunter run works (covered in 6/7).
6. Admin → Default workbook ⋮/header "Credit limit…" → limit 1 daily → badge "limit: 1/daily". Open Prospects tab → Work email ▶ Run column (all 21 rows, mock) → ≥1 cell done then rest `skipped`; cell drawer error "Credit limit reached for workbook Default workbook". Remove limit: Credit limit… → clear? (check: no delete in dialog → use /credits Budgets Delete) → badge gone → rerun column → all done. FAIL if no skipped cells with that exact message or if badge persists.
7. Regression (admin): home cards/tags/folders render, search box finds "Hunter", Hunter view "Anthropic execs" still 9 of 80. No native alert/confirm/prompt anywhere (hook window.* and check).
