# PR #4 @ f273635 — Workbooks / Views / Tags (UI, running stack)

IDs: Default workbook `7d0e6264b7f0d2e2714a13653f524435` (9 tables), Q3 campaigns wb `cmtouc9oy0004qhh6n0s0q547` in folder "Q3 campaigns" `cmtouc9ox0002qhh61x4aq3vt`. Hunter table `cmtorv0hq0003qhe9nwhf0rve` (80 rows; Company=Anthropic ×10; Seniority executive/senior). Tags: Active, SDR team.
Code: home `apps/web/app/page.tsx` (＋Folder/＋Workbook dialogs, ⋮ Rename/Move/Delete, Move takes Folder ID text), workbook page `workbooks/[id]/page.tsx` (tabs, ⋮ Rename/Move/Move left/right/Delete, ＋Add table with optional Template ID), table page view toolbar `tables/[id]/page.tsx:790-985` (editActiveView on Default → "Save as view" prompt 352), export uses `?viewId` (655), fanout modal scope radios (1712). API delete-last: `workbooks.controller.ts:126`.

1. Home `/`: ＋Folder "QA folder" → appears in tree. Select it → ＋Workbook "QA wb" → card with 0 tables under QA folder. ⋮ Rename → "QA wb renamed" → card title updates. ⋮ Move → Folder ID `cmtouc9ox0002qhh61x4aq3vt` → card disappears from QA folder, appears under "Q3 campaigns" folder. Tag picker on card: create new tag "Hot" with a colour → chip appears in top tag row with dot colour; assign existing "Active". Click chip "Hot" → only "QA wb renamed" card shown; click "Active" → QA wb + Q3 campaigns (if tagged) shown; "All tags" → all. FAIL if filter shows untagged workbooks.
2. Open Default workbook → tab "Clay users — GTM contacts (Hunter)" (80). ＋New view "Anthropic execs" → URL `?view=<id>`, select shows it. Filter: Company eq Anthropic, ＋Add filter Seniority eq executive, Match All → Apply → row label "N of 80 rows" with N<10 and every visible row Company=Anthropic & Seniority=executive. Sort Last name Ascending → Apply → Last names alphabetical. Hide columns → uncheck LinkedIn → LinkedIn header gone. Select Default view → 80 rows, LinkedIn header back, URL has no ?view. Switch back to "Anthropic execs" → filtered again. Duplicate → new option "Anthropic execs (copy)" (or similar) selected; Rename → "Execs copy"; Delete → confirm dialog (in-app) → option gone, falls back to Default.
3. On Default view: Filter Company eq Anthropic → Apply → in-app dialog "Save as view" appears (NOT applied silently). Cancel → still 80 rows, no view created. FAIL if rows filtered without prompt.
4. In "Anthropic execs" view (embedded in workbook): select 2 rows → Find people → modal mentions visible/view rows (record exact text) → mock.findPeople limit 1 → toast "Found 2 people across 2 companies" → new tab appears in Default workbook tabs (name "…— people"). Export CSV from the view → file in ~/Downloads has header + N data rows (N = filtered count), not 80.
5. Workbook page: ＋Add table name "QA blank" (no template) → tab appears, 0 rows, active. ＋Add table "QA tpl" with template id `builtin-people-outreach` → tab with People outreach columns. Tab ⋮ Move on "QA tpl" → Destination workbook ID `cmtouc9oy0004qhh6n0s0q547` → tab disappears; open Q3 campaigns workbook → tab present. Back in Default: "QA blank" ⋮ Move left → tab order changes (screenshot before/after). ⋮ Delete "QA blank" → confirm → tab gone. Delete-last: on home delete all workbooks except one is destructive — instead: use a fresh workspace? Not available; so delete the QA workbook (allowed) and then verify refusal by attempting delete on the remaining single workbook only if count=1 — otherwise verify refusal via UI is impossible; fallback: check API response text "Cannot delete the last workbook in a workspace" (400) and whether UI surfaces it (home page delete has no error toast → record as finding).
6. Regression: Hunter table column ▶ Run on "Work email" (or a cheap mock column) → cells go done. /audiences and /sequences load. No native alert/confirm/prompt anywhere (all dialogs in-app).
