# PR #3 @ f880e2e — templates/import-source/connections (UI, reuse running stack, no reseed)

Login demo@gtmai.dev / demo1234. Don't delete "OpenAI" connection.

1. /templates → "Find companies using Clay" → Use template → lands on new table (templates.controller.ts seeds rows; template definition find-clay-users.json: 30 rows, columns Company, Domain, Clay research(agent), Uses Clay?, Evidence URL, Confidence, Evidence(formula)). PASS: header shows 30 rows, Company/Domain filled, 7 columns with those names. Click ONE "Clay research" cell → drawer → "↻ Re-run cell" (tables/[id]/page.tsx:1188-1196 runs with rowIds:[row]). PASS: within ~90s only that cell → done (others remain), Uses Clay?/Evidence URL/Confidence/Evidence formula cells fill for that row; reopen drawer: value JSON shows fields + sources, Provenance "Available". FAIL if whole column runs or formulas stay empty.
   Also open /tables/cmto29j090001qhgtjldk4i8a and screenshot grid state only.
2. /templates → "Find companies by technology" → Use template → table with Company/Domain/Industry/Employees/Country/Tech stack, 0 rows. Click "Import from source" (page.tsx:462) → modal (1104-1155) lists search actions incl. "theirstack · Find companies by technology" and dynamic fields (technology, country, min/max employees, limit). Enter technology "hubspot" → Import companies. PASS: error toast "No connection for theirstack — add one in Connections"; no window.alert/prompt/confirm anywhere in flows.
3. /connections → ＋ Add connection: provider select from /connections/catalog (connections/page.tsx:121-155). Select LLM → fields "API key" + "Tavily API key (optional …)" marked (optional). Select TheirStack → single "API key" field. Select mock → create "Retest mock conn" → listed → Delete. Do not save LLM.
