# PR #3 @ 41380a5 — "Find people" fan-out (UI, reuse running stack)

Source table: new Clay table /tables/cmto3y2dr001zqh0nx9rvuiic (30 rows Company/Domain, Uses Clay?, Evidence URL). Login demo@gtmai.dev / demo1234.

1. Select rows Anthropic + OpenAI (checkboxes) → toolbar "Find people" (page.tsx:549) → modal (1248-1336): action select shows mock.findPeople/apollo/hunter; domain binding prefilled `{{Domain}}`; Limit field; radio "Selected rows (2)" / "All rows"; carry checkboxes Company, Domain, Uses Clay?, Evidence URL checked; target name "Find companies using Clay — people". Set Limit 2, name "Clay people sel" → Find people. PASS: toast "Found 4 people across 2 companies"; navigates to new table with 4 rows; First name/Last name/Title/Email (`<first>@anthropic.com`/`@openai.com`)/LinkedIn filled; Company/Domain/Uses Clay?/Evidence URL carried from parent (Anthropic row: Uses Clay?=unknown). Columns include Work email (waterfall), Best email (formula), Personalization (agent). FAIL if row count ≠ 4 or carried values empty.
2. Back on source table → Find people, scope "All rows", Limit 1, name "Clay people all" → PASS toast "Found 30 people across 30 companies", new table 30 rows.
3. On "Clay people sel" table: Work email column ▶ Run → PASS within ~20s all 4 cells done with provider mock (drawer Provider mock), no error cells for hunter/prospeo missing connections (executors.ts:146-150 skip); Best email cells show plain email string e.g. `ada@anthropic.com`, not `{"email":…}` JSON.
4. Source table → Find people → action apollo.peopleSearch → Find people → PASS error toast "No connection for apollo — add one in Connections" (filter maps Error→400); modal stays open; no "Internal server error".
5. Throughout: no window.alert/confirm/prompt dialogs.
Optional: single-cell re-run of Personalization via cell drawer on one row (OpenAI).
