# Phase 1 E2E Test Plan (gtmai PR #1)

Environment: web :3000, API :4000 (pid 50326), worker restarted with correct ENCRYPTION_KEY (previous worker had stale 48-char key → all Work email cells "Invalid key length"). Seed DB intact (20 rows, 6 columns, 1 mock connection, 0 ledger entries).

Code refs: apps/web/app/tables/[id]/page.tsx (toolbar L382-409, header Run L434, drawer L932-970, add-column wizard L596-866, import modal L867-930), apps/web/app/login/page.tsx, apps/web/app/connections/page.tsx, apps/web/app/credits/page.tsx.

## T1 Auth
1. Clear localStorage, go to /login, log in demo@gtmai.dev/demo1234 → PASS: redirected to Tables page listing "Prospects".
2. Sign out (SignOutFooter in sidebar) → PASS: back at /login; visiting / redirects to /login.
3. Toggle to Register, create tester+<ts>@gtmai.dev / test12345 → PASS: lands on Tables (empty workspace or new). Sign out, log back in as demo.

## T2 Prospects grid
1. Open Prospects. Initial state: Work email cells show "Invalid key length" (stale worker). Click header ▶ Run on Work email → PASS: within ~10s cells fill with distinct emails (e.g. ada.lovelace@... differs per row) with no page refresh. FAIL if same email or still error.
2. Display name column = "<First> <Last>" per row. AI summary cells show "No connection for openai — add one in Connections".

## T3 Add row + live SSE
1. Click "＋ Row" → new 21st row appears (row count "21 rows"). Type First name "Grace", Last name "Hopper", Domain "navy.mil" in the inputs (wait 400ms debounce each).
2. Click header ▶ Run on Work email → PASS: new row's Work email fills (contains "grace"/"hopper" or navy.mil) without reload; other rows unchanged.

## T4 Cell drawer
Click a Work email cell → drawer shows JSON with email, Status done, Provider mock, Credits >0, Provenance Available. Click "↻ Re-run cell" → cell goes to pending/running then done again.

## T5 Add column
a. Formula: Add column → name "Last upper", kind formula → Next → expression `upper({{Last name}})`, "Preview against row 1" toast shows uppercased last name → Next → Create column. Header Run → PASS: cells show uppercase last names.
b. Waterfall: Add column → name "Phone", kind waterfall → Next → Action "mock.findPhone" → "+ Add provider step" (now 2 steps), click ↑ on step 2 to reorder → PASS: order swaps → Next → Create. ⋮ → Run empty → PASS: cells fill with phone values. Verify config input auto-mapped domain via DB/drawer.

## T6 Claygent preview
Edit AI summary (✎ Edit) → Next → "Test on first 3 rows" → PASS: preview pre shows 3 entries with structured error mentioning no connection for openai. FAIL if blank/crash.

## T7 CSV
a. Export CSV → PASS: Prospects.csv downloaded to ~/Downloads with header row and 21+ rows.
b. Import via paste: "First name,Last name,Domain\nLinus,Torvalds,kernel.org" → Import → row count +1 with values.
c. Import via file: create /tmp/import.csv with headers "given,surname,site" → upload → mapping selects appear; map to First name/Last name/Domain → Import → row count +N, values in correct columns.

## T8 Connections & Credits
Connections page: "Mock Provider" exists; click Test → success message. Credits page: Balance/ledger shows entries from mock runs (count >0, provider mock).

## T9 Swagger
http://localhost:4000/docs renders Swagger UI with endpoints listed.
